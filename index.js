'use strict';

//initialize variables
var Seed = Math.random();
var PreviewWidth = 200;
//this value needs to be set by the user
//it is the directory of torbenm's planet generator on their local machine
var TPDir = "";

require("./generateMap.js");
